<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml for AR Badminton Net
    Declares Unity's main activity as the launcher and adds the transparent
    helper activity used for MediaProjection permission prompts.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arbadmintonnet">

    <!-- Required for MediaProjection screen recording -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <application>
        <!-- Unity's main activity with all required attributes -->
        <activity
            android:name="com.unity3d.player.UnityPlayerGameActivity"
            android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|fontScale|layoutDirection|density"
            android:enabled="true"
            android:exported="true"
            android:hardwareAccelerated="false"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:screenOrientation="fullUser"
            android:theme="@style/BaseUnityGameActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
        </activity>

        <!-- Transparent helper activity for MediaProjection permission dialog only -->
        <activity
            android:name="com.arbadmintonnet.replay.ScreenRecorderActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Foreground service required for MediaProjection on Android 10+ -->
        <service
            android:name="com.arbadmintonnet.replay.ScreenRecorderService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />
    </application>
</manifest>
